import * as React from 'react';
import { View, Text, StyleSheet, TouchableOpacity } from 'react-native'

export default class DailyTips extends React.Component {
    render() {
        return(
            <View>
                <Text>DailyTips</Text>
            </View>
        )
    }
}

const styles = StyleSheet.create({

})

/* Daily Tips allows you to gain knowledge before starting a Work Time session. It tells you a few tips,
instructions and key points before starting your productive and focused sessions. It's a popup on your screen! */