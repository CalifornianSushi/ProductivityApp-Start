import * as React from 'react';
import { View, Text, StyleSheet, TouchableOpacity } from 'react-native'

export default class HabitTracker extends React.Component {
    render() {
        return(
            <View>
                <Text>HabitTracker</Text>
            </View>
        )
    }
}

const styles = StyleSheet.create({

})

/* List your daily habits & routines. The app will remind you to do them! Daily Habits can also be viewed in the Home Screen
Track your habits and make sure you do them all! */